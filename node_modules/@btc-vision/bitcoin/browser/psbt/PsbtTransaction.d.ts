import { Transaction as ITransaction, TransactionFromBuffer } from 'bip174';
import { Transaction } from '../transaction.js';
import { PsbtTxOutput, TransactionInput } from './types.js';
/**
 * This class implements the Transaction interface from bip174 library.
 * It contains a bitcoinjs-lib Transaction object.
 */
export declare class PsbtTransaction implements ITransaction {
    tx: Transaction;
    constructor(buffer?: Uint8Array);
    getInputOutputCounts(): {
        inputCount: number;
        outputCount: number;
    };
    addInput(input: TransactionInput): void;
    addOutput(output: PsbtTxOutput): void;
    toBuffer(): Uint8Array;
}
/**
 * This function is needed to pass to the bip174 base class's fromBuffer.
 * It takes the "transaction buffer" portion of the psbt buffer and returns a
 * Transaction (From the bip174 library) interface.
 */
export declare const transactionFromBuffer: TransactionFromBuffer;
//# sourceMappingURL=PsbtTransaction.d.ts.map