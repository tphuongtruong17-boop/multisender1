import { fromBase64, fromHex } from '@btc-vision/bitcoin';
export function stringToBuffer(str) {
    return fromHex(str.startsWith('0x') ? str.slice(2) : str);
}
export function stringBase64ToBuffer(str) {
    return fromBase64(str);
}
